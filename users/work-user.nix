{
  home-manager.users.mkantzer.programs.git = {
    userName = "mkantzer";
    userEmail = "mike.kantzer@drizly.com";
  };

  users.users.mkantzer = {
    # isNormalUser = true;
    # extraGroups = [ "wheel" ];
  };
}
