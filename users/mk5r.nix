{
  home-manager.users.mk5r.programs.git = {
    userName = "mk5r";
    userEmail = "github@kantzer.io";
  };

  users.users.mk5r = {
    # isNormalUser = true;
    # extraGroups = [ "wheel" ];
  };
}
